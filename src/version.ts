/**
 * Server version, reported to MCP clients in the initialize handshake.
 *
 * GENERATED — do not edit by hand. `make version-sync` (run by the release
 * targets) rewrites this from package.json. The mcpb bundle strips
 * package.json, so the version has to be baked in at build time rather than
 * read at runtime. version.test.ts fails the build if this drifts.
 */
export const VERSION = '0.7.2';
